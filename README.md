# Samiul Alam — Academic Website

A responsive, dependency-free academic portfolio website built from the supplied CV.

## Files
- `index.html` — website content
- `styles.css` — visual design and responsive layout
- `script.js` — mobile navigation, publication filters, and subtle reveal effects
- `assets/Samiul_Alam_CV.docx` — downloadable CV
- `assets/favicon.svg` — site icon

## Preview locally
Double-click `index.html`, or run a simple local server from this folder:

```bash
python -m http.server 8000
```

Then open `http://localhost:8000`.

## Publish with GitHub Pages
1. Create a public GitHub repository, for example `samiul-alam.github.io`.
2. Upload the contents of this folder to the repository root.
3. In GitHub, open **Settings → Pages**.
4. Set the source to **Deploy from a branch**, choose `main`, and choose `/ (root)`.
5. Save. GitHub will show the published URL after deployment.

If your repository is named exactly `USERNAME.github.io`, the site URL will be `https://USERNAME.github.io/`.

## Recommended next edits
- Replace the `SA` initials card with a professional photo if desired.
- Add Google Scholar, ORCID, ResearchGate, LinkedIn, and GitHub URLs once available.
- Replace the DOCX download with a PDF CV if preferred.
- Add research figures / graphical abstracts to the project cards.
- Update publication counts whenever new work is added.

## Public privacy choice
The site intentionally omits the street address and phone number from the CV. It shows only the institutional email and city/state location.
